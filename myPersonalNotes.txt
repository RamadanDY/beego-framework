Install Bee
Run:

>>>>go get -u github.com/beego/bee/v2@latest
For Go 1.18 and newer Go versions run:

>>>>>go install github.com/beego/bee/v2@latest


Check where Go installed bee

Run this command:

ls ~/go/bin

You should see something like:

bee

If you see bee, then it means Go installed it here:

~/go/bin/bee
2. Add Go bin to your PATH

Your terminal cannot find bee because ~/go/bin is not in your PATH.

Run this:

echo 'export PATH=$PATH:~/go/bin' >> ~/.bashrc

Then reload your terminal configuration:

source ~/.bashrc
3. Check again

Now run:

bee version

Create your first Beego project

Once bee works, you can create a project:

bee new myapp

Then enter the folder:

cd myapp

Run the application:

bee run

Your server will start on:

http://localhost:8080



////////////////all the varios logic in the create route 

///normal error man with just logging wrong input in the req body 
// func (c *BookController) Create() {
// 	var book models.Book

// 	//lets parse the json from the req data 

// 	err := json.Unmarshal(c.Ctx.Input.RequestBody, &book) 
// 	if err != nil {
// 		//we return an error response ie 
// 		c.Data["json"] = map[string]string{"error": "invalid JSON"} 
// 		c.ServeJSON()
// 		return
// 	}
// 	models.Books = append(models.Books,book)
// 	c.Data["json"] = book
// 	c.ServeJSON()

// }





// func (c *BookController) Create() {

// 	var book models.Book

// 	err := json.Unmarshal(c.Ctx.Input.RequestBody, &book)
// 	if err != nil {
// 		c.Data["json"] = map[string]string{"error": "Invalid JSON"}
// 		c.ServeJSON()
// 		return
// 	}

// 	models.Books = append(models.Books, book)

// 	c.Data["json"] = book
// 	c.ServeJSON()
// }




/////lets now create a book and add it to the slice  ,error handling excluded
//json 
// func (c *BookController) Create() {

// 	var book models.Book
// 	json.Unmarshal(c.Ctx.Input.RequestBody, &book)

// 	models.Books = append(models.Books, book)

// 	c.Data["json"] = book
// 	c.ServeJSON()
// }


/////lets now create a book and add it to the slice  ,error handling included
//this is for form type 
// func (c *BookController) Create() {
// 	//lets create a var to keep the new book and its based on the book struct 

// 	var book models.Book

// 	//we then parse the req data 
// 	c.ParseForm(&book)

// 	//add book to slice 
// 	models.Books = append(models.Books,book)

//  	c.Data["json"] = book
//  	c.ServeJSON()

// }
